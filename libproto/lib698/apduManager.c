#include "apduManager.h"

boolean userDataManager(u8* buf, u16 bufSize, frmHead_s* pFrmHead)
{
    u8 ret = TRUE;

    NOT_USED(buf);
    NOT_USED(bufSize);
    NOT_USED(pFrmHead);

    return ret;
}
