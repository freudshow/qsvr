#include "a_xdr.h"


int parseSequenceOf(u8* buf, u32 buflen)
{
    UNUSEDV(buf);
    UNUSEDV(buflen);
    return 0;
}

int composeSequenceOf(u8* buf, u32 buflen)
{
    UNUSEDV(buf);
    UNUSEDV(buflen);
    return 0;
}
